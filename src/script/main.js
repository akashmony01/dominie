// slider activation
// -------------------------------------------------------
jQuery(document).ready(function($) {
	$('.owl-carousel').owlCarousel({
	  center: true,
	  dots: false,
	  loop: true,
	  margin: 50,
	  autoWidth:true
	});
});